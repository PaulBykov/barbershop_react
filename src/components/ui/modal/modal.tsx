import React from 'react';

function Modal() {
    return (
        <dialog>
            <button onClick={() => .close() }>
                X
            </button>

            <form action="">
                <select name="" id=""></select>
                <select name="" id=""></select>
                <input type="date"/>
            </form>

        </dialog>
    );
}

export default Modal;